angular.module('currencyConverter').component('navigation', {
    templateUrl: 'app/components/navigation/navigation.html',
    controller: NavigationController
});

function NavigationController() {
	var self = this;

	self.$onInit = function() {
	};
}